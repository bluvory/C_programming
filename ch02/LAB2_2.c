# include <stdio.h>

int main(void) {

	int radius;
	radius = 5;

	printf("When the radius is %d, the area is %f\n", radius, radius*radius*3.14);
	printf("When the radius is %d, the circumference is %f", radius, radius * 2 * 3.14);
}